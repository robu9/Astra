{
  "crm_at_risk@crm": {
    "active": "When determining 'at risk' status, consider deals with `health: 'R'`, deals with notes indicating risk (e.g., 'budget freeze'), AND deals whose owner is inactive (check `crm.get_owner` for `active: false`). If an owner is inactive, the deal is 'UNASSIGNED'. Ensure `crm.get_owner` is called for every deal with an owner ID to get the name and check activity status.",
    "candidate": null,
    "history": [
      {
        "text": "To find 'OPEN' deals, filter for stages like 'qualified' or 'negotiation'. 'At-risk' deals are those with `health: 'R'`. Remember to call `crm.get_owner` for each deal's owner_id.",
        "run": "crm_at_risk-r01",
        "kept": true,
        "resolved_run": "crm_at_risk-r02"
      },
      {
        "text": "To find all 'OPEN' deals, call `crm.list_deals` for *all* relevant stages like 'qualified', 'negotiation', 'prospecting', and 'proposal'. For each deal, call `crm.get_deal` to confirm its 'at risk' status and `crm.get_owner` to get the owner's name. The `health: 'R'` rule might be insufficient; check `crm.get_deal` for more details.",
        "run": "crm_at_risk-r02",
        "kept": true,
        "resolved_run": "crm_at_risk-r03"
      },
      {
        "text": "The `health: 'R'` rule is insufficient for identifying all at-risk deals. For each deal, you MUST call `crm.get_deal` to determine its full 'at risk' status. If `crm.get_deal` does not provide a clear reason, consider calling `crm.get_notes` for additional context. Only query `crm.list_deals` with known valid stages like 'qualified' and 'negotiation'; 'prospecting' is not a valid stage.",
        "run": "crm_at_risk-r03",
        "kept": true,
        "resolved_run": "crm_at_risk-r04"
      },
      {
        "text": "The `health: 'R'` rule is insufficient for identifying all at-risk deals. For each deal, you MUST call `crm.get_deal` AND `crm.get_notes` to determine its full 'at risk' status and the reason. Only query `crm.list_deals` with known valid stages like 'qualified' and 'negotiation'; 'prospecting' is not a valid stage.",
        "run": "crm_at_risk-r04",
        "kept": true,
        "resolved_run": "crm_at_risk-r05"
      },
      {
        "text": "When determining 'at risk' status, consider deals with `health: 'R'`, deals with notes indicating risk (e.g., 'budget freeze'), AND deals whose owner is inactive (check `crm.get_owner` for `active: false`). If an owner is inactive, the deal is 'UNASSIGNED'. Ensure `crm.get_owner` is called for every deal with an owner ID to get the name and check activity status.",
        "run": "crm_at_risk-r05",
        "kept": true,
        "resolved_run": "crm_at_risk-r01"
      }
    ],
    "active_before_candidate": "The `health: 'R'` rule is insufficient for identifying all at-risk deals. For each deal, you MUST call `crm.get_deal` AND `crm.get_notes` to determine its full 'at risk' status and the reason. Only query `crm.list_deals` with known valid stages like 'qualified' and 'negotiation'; 'prospecting' is not a valid stage."
  },
  "tracker_triage@tracker": {
    "active": "When triaging issues, if the module is 'billing', 'exports', 'mobile', 'auth', or 'search', or if the title contains 'Data loss' or 'sync is broken' for the 'exports' module, or if 'Export to CSV fails' is in the title, or if no clear label rule applies, call `tracker.get_issue` and `tracker.list_comments` for the issue to gather more context before deciding on the assignee and labels. Do not assume 'Export to CSV fails' or 'sync is broken' implies 'sev-2' without further evidence. Ensure all issues from `list_issues` are processed and assigned a label.",
    "candidate": {
      "text": "When triaging issues, if the module is 'billing', 'exports', 'mobile', 'auth', or 'search', or if the title contains 'Data loss' or 'sync is broken' for the 'exports' module, or if 'Export to CSV fails' is in the title, or if no clear label rule applies, call `tracker.get_issue` and `tracker.list_comments` for the issue to gather more context before deciding on the assignee and labels. Do not assume 'Export to CSV fails' or 'sync is broken' implies 'sev-2' without further evidence. Ensure all issues from `list_issues` are processed and assigned a label.",
      "run": "tracker_triage-r05"
    },
    "history": [
      {
        "text": "After determining the correct labels and assignees, remember to call `tracker.add_label` and `tracker.assign_issue` for each issue. Do not just report the plan; execute it.",
        "run": "tracker_triage-r01",
        "kept": true,
        "resolved_run": "tracker_triage-r02"
      },
      {
        "text": "When triaging issues, if the module is 'billing' or 'mobile', or if no clear label rule applies, call `tracker.get_issue` and `tracker.list_comments` for the issue to gather more context before deciding on the assignee and labels. Do not assume 'Export to CSV fails' or 'sync is broken' implies 'sev-2' without further evidence.",
        "run": "tracker_triage-r02",
        "kept": true,
        "resolved_run": "tracker_triage-r03"
      },
      {
        "text": "When triaging issues, if the module is 'billing', 'exports', or 'mobile', or if the title contains 'Data loss' or 'sync is broken' for the 'exports' module, or if no clear label rule applies, call `tracker.get_issue` and `tracker.list_comments` for the issue to gather more context before deciding on the assignee and labels. Do not assume 'Export to CSV fails' or 'sync is broken' implies 'sev-2' without further evidence. Ensure all issues from `list_issues` are processed.",
        "run": "tracker_triage-r04",
        "kept": true,
        "resolved_run": "tracker_triage-r05"
      }
    ],
    "active_before_candidate": "When triaging issues, if the module is 'billing', 'exports', or 'mobile', or if the title contains 'Data loss' or 'sync is broken' for the 'exports' module, or if no clear label rule applies, call `tracker.get_issue` and `tracker.list_comments` for the issue to gather more context before deciding on the assignee and labels. Do not assume 'Export to CSV fails' or 'sync is broken' implies 'sev-2' without further evidence. Ensure all issues from `list_issues` are processed."
  },
  "gh_bug_triage@github": {
    "active": "The tool github.list_collaborators is not available. To determine 'maintainer_replied', you must call github.get_issue for each issue to check for comments, but there is no tool to list repository collaborators. Therefore, 'maintainer_replied' cannot be accurately determined with the current tools. Ensure 'age_days' is calculated accurately using the 'created_at' field from the issue object and today's date (2026-09-06).",
    "candidate": {
      "text": "The tool github.list_collaborators is not available. To determine 'maintainer_replied', you must call github.get_issue for each issue to check for comments, but there is no tool to list repository collaborators. Therefore, 'maintainer_replied' cannot be accurately determined with the current tools. Ensure 'age_days' is calculated accurately using the 'created_at' field from the issue object and today's date (2026-09-06).",
      "run": "gh_bug_triage-r03"
    },
    "history": [
      {
        "text": "To calculate 'age_days', use the 'created_at' field from github.list_issues. For 'maintainer_replied', you MUST call github.get_issue for each issue to inspect its comments and their 'author_association' field (e.g., 'COLLABORATOR', 'MEMBER', 'OWNER').",
        "run": "gh_bug_triage-r01",
        "kept": false,
        "resolved_run": "gh_bug_triage-r02"
      },
      {
        "text": "You must process all issues returned by github.list_issues, not just a subset. For 'maintainer_replied', first call github.list_collaborators for the repo. Then, for each issue, call github.list_issue_comments to get all comments and check if any comment author is a collaborator. Ensure 'age_days' is calculated accurately using the 'created_at' field and today's date (2026-09-06).",
        "run": "gh_bug_triage-r02",
        "kept": true,
        "resolved_run": "gh_bug_triage-r03"
      }
    ],
    "active_before_candidate": "You must process all issues returned by github.list_issues, not just a subset. For 'maintainer_replied', first call github.list_collaborators for the repo. Then, for each issue, call github.list_issue_comments to get all comments and check if any comment author is a collaborator. Ensure 'age_days' is calculated accurately using the 'created_at' field and today's date (2026-09-06)."
  },
  "gh_bug_digest@github": {
    "active": "When calculating 'age_days', ensure the difference in days is correctly computed as an integer, where 'created_at' on 2026-09-05 and 'today' on 2026-09-06 yields 1 day. For 'assigned' status, explicitly check if the 'assignees' list is empty or not. Ensure all issues returned by github.list_issues (up to 'per_page') are processed.",
    "candidate": null,
    "history": [
      {
        "text": "After calling github.list_issues with per_page: 10, process the results directly. Do not make additional calls with smaller per_page values. Extract all required fields (number, title, comments, assignees, labels) from the initial response and calculate age_days and other_labels. Then, format the final digest.",
        "run": "gh_bug_digest-r01",
        "kept": true,
        "resolved_run": "gh_bug_digest-r02"
      },
      {
        "text": "When calculating 'age_days', ensure the difference in days is correctly computed as an integer, where 'created_at' on 2026-09-05 and 'today' on 2026-09-06 yields 1 day. For 'assigned' status, explicitly check if the 'assignees' list is empty or not. Ensure all issues returned by github.list_issues (up to 'per_page') are processed.",
        "run": "gh_bug_digest-r03",
        "kept": true,
        "resolved_run": "gh_bug_digest-r04"
      }
    ],
    "active_before_candidate": "After calling github.list_issues with per_page: 10, process the results directly. Do not make additional calls with smaller per_page values. Extract all required fields (number, title, comments, assignees, labels) from the initial response and calculate age_days and other_labels. Then, format the final digest."
  }
}
